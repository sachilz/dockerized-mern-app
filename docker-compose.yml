services:
  frontend:
    build: ./frontend
    environment:
      - REACT_APP_BACKEND_URL=/api

  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    restart: always

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - frontend
      - backend
    restart: always